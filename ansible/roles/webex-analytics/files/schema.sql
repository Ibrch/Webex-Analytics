CREATE TABLE IF NOT EXISTS cdr_raw (
    report_id UUID PRIMARY KEY,
    answer_time TIMESTAMP,
    answered VARCHAR,
    direction VARCHAR,
    call_id VARCHAR,
    start_time TIMESTAMP,
    call_type VARCHAR,
    client_type VARCHAR,
    correlation_id UUID,
    duration BIGINT,
    org_uuid UUID,
    user_uuid UUID,
    user_type VARCHAR,
    user_name VARCHAR,
    called_number VARCHAR,
    calling_number VARCHAR,
    "location" VARCHAR,
    dialed_digits VARCHAR,
    releasing_party VARCHAR,
    redirecting_number VARCHAR,
    site_uuid UUID,
    user_number VARCHAR,
    call_outcome VARCHAR,
    call_outcome_reason VARCHAR,
    ring_duration VARCHAR,
    answer_indicator VARCHAR,
    release_time TIMESTAMP,
    pstn_vendor_name VARCHAR,
    external_customer_id VARCHAR,
    redirecting_party_uuid VARCHAR,
    original_called_party_uuid VARCHAR,
    queue_type VARCHAR,
    answered_elsewhere VARCHAR,
    hold_duration BIGINT,
    interaction_id UUID,
    transfer_type VARCHAR,
    transfer_type_context VARCHAR,
    source_file VARCHAR,
    loaded_at TIMESTAMP
);
